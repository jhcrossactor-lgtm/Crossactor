# MEMORY — Crossactor 重要決定記録

セッション開始時に必ず読む。重要な決定は「何を決めたか・理由・却下した選択肢」の形で即記録する。

---

## 2026-09-16 — Inspo MCP をデザイン系スキルの前工程に組み込む

**決定**
LP・販促クリエイティブ・Web UI を実装する前に、Inspo MCP で実在サイトを3〜5件参照することを
promo-design / event-lp / seo-lp の3スキルに義務付けた。標準手順は `search_screens` → `get_design_system`。

**理由**
配色・フォント・版面構成を毎回ゼロから発想すると品質が振れる。実在サイトを根拠にすれば下限が上がる。
ツール名を明示したのは、抽象的な「Inspo MCPで調べる」だと実際に呼ばれない確率が高いため。

**制約（重要）**
Inspo MCP は **ローカル実行時のみ利用可能**。Claude Code on the web / クラウドセッションは
egress ポリシーが `inspomcp.dev:443` を 403 で拒否する。
→ 取得できない場合はそのまま続行し、成果物に「Inspo未参照」と明記する運用にした。

**却下した選択肢**
- 貼り付け用テキストのみ（a）→ リポジトリに記録が残らず再現できないため却下
- リポジトリコミットのみ（b）→ `~/.claude/skills/synced/` は claude.ai の一方向ミラーで
  書き換えてもアカウント側に反映されない。貼り付け作業が必須なため却下
- → **両方（C）を採用**

**対象外**
- `frontend-design` スキルは存在しないため対象外
- seo-lp は分析専用スキルのため「デザイン改修まで踏み込む場合のみ」の条件付き

**適用範囲（2重化）**
1. claude.ai のスキル3本（2026-09-16 反映済み）— スキル発火時にカバー
2. リポジトリ `CLAUDE.md` の「デザイン実装前の必須手順」— スキルが発火しないデザイン作業をカバー

**Croにできないこと（再発防止のため記録）**
claude.ai アカウント側スキルへの書き込み手段は存在しない（CLI・プラグイン経由とも不可）。
スキル本体の修正が必要な場合、貼り付けは必ずほせもやんが行う。Croは差分と全文を用意するところまで。

**記録場所**
`skills/inspo-mcp/README.md`（導入手順・ツール15件一覧・貼り付け用ブロック）
`skills/inspo-mcp/patched/`（追記済みSKILL.md全文）
`communications/logs/2026-09-16.md`（経緯）

---

## 2026-09-24 — AI相棒クロス Phase1 開始（`cross/`）

**決定**
- 音声対話AI「クロス」を `cross/` 配下に新規プロジェクトとして開始。仕様は `cross/CLAUDE.md`
- ステップ順：①テキスト会話 → ②音声合成 → ③音声認識 → ④3D部屋＋2Dキャラ。各ステップでほせもやんの動作確認を待ってから次へ進む
- Step① は実装済み。ほせもやんの動作確認待ち

**理由・設計判断**
- `G:\ClaudeLocal\cross` はクラウド環境から到達不能 → リポジトリ内 `cross/` に置く。独立リポジトリにしたければフォルダごとコピーして `git init`
- system prompt は `persona.md`+`knowledge.md` を `scripts/deploy.mjs` で `prompt.ts` に焼く（Edge Function から外部ファイルを読む手段が不安定なため）
- 上位モデルは thinking オフ + effort low（遅延優先）。既定 Haiku 4.5 は effort 非対応

**却下した選択肢**
- `verify_jwt = false` の公開エンドポイント → Anthropic キーの無断消費リスクで却下
- Supabase `static_files` で md を同梱 → 動作保証が取れず却下

**注意**
- `.env` と `config.local.js` は gitignore。キーは絶対にコミットしない
- Azure の日本語音声に関西弁ネイティブは無い。Step② はイントネーション妥協前提

## 2026-09-26 — クロスのバックエンドは Supabase で確定（ローカルNode案は却下）

**決定**
撮影用途に加えて実用（スマホから話しかける）を見据えるため、Supabase Edge Functions を維持する。

**理由**
PCの外から使うには HTTPS の常設エンドポイントが要る。ローカル Node サーバーではスマホから届かない。

**却下した選択肢**
- ローカル Node サーバー（設定ゼロで速いが、PC外から使えない）。一度削除まで進めたが、ほせもやんの判断で復元

**派生タスク（Phase1 完了後）**
- スマホ版：index.html を HTTPS で公開（GitHub Pages 等）し、ホーム画面追加（PWA）で「アプリ的」に使う。縦画面用の軽量レイアウトが要る
- 公開URLにする前に `/chat` へ合言葉チェックを追加（anon key だけでは誰でも叩ける）

## 2026-09-26 — クロスは PC・スマホ同一画面、スキン選択式、将来パッケージ販売

**決定**
- PCでもスマホでも同じ16:9画面。スマホは横持ち（PWAで横向き固定）
- スキンを `config.js` で選択：`room`（部屋＋キャラ）／`orb`（Jarvis風球体）。orb は軽量モード兼用
- パッケージ販売はセルフホスト型：顧客が自分のアカウント（Anthropic / Supabase / Azure）で自分の端末に構築。Crossactor 側にインフラ・顧客データを持たない。README と手順が商品の一部
- 固有情報はすべて設定ファイル側に置き、クロスと Crossactor ロゴは同梱サンプル扱い

**理由**
ほせもやんの要望「PCでもスマホでも同じものにしたい。選べるようにしたい。スマホは横で使ってもらう」。
販売を考えると画面を端末ごとに分けるより1本に固定した方が保守もデモも楽。

**却下した選択肢**
- スマホ専用の縦画面レイアウト（球体のみ）→ PC版と別物になり、商品として二重管理になるため却下
- ネイティブアプリ先行 → Web で9割叶う。必要になれば Capacitor で包む

**実装順への影響**
Phase1 の順（①会話 → ②TTS → ③STT → ④部屋とキャラ）は変えない。④の中で orb スキンも同時に作る。

## 2026-09-26 — クロス用 Anthropic APIキー
- 名前 `Cross`、デフォルトワークスペース所属、**有効期限 2027-12-31 14:00 JST**。期限前に更新が必要（2027年12月上旬に通知すること）
- Supabase はクロス専用プロジェクト `cross`（Tokyo）を新規作成する方針

## 2026-09-26 — 教訓：シークレットを含むコマンドは絶対に画面表示しない
deploy.mjs が `supabase secrets set KEY=値` をそのまま echo し、ほせもやんのスクショにキーが写った。
以後、キー・トークンを含む文字列はログ・echo・エラーメッセージのいずれにも出さない。表示が必要なら先頭4文字＋末尾2文字のみ。
当該キーは削除・再発行済み（ほせもやん対応）。

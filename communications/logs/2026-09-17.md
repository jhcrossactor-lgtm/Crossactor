# 2026-09-17

## ヴィラPR動画パイプライン構築（社内テスト）

### ほせもやんからの指示
写真とMOVから16:9のPR動画（約35秒）を作るパイプラインを Python + ffmpeg で構築。
画像＝OpenAI画像編集API、動画＝Gemini Omni 1.1 Flash または MiniMax H3（config切替）。
素材は Google Drive の `★ Crossactor / AI関連 / test`。
モデル名・エンドポイントは推測せず最新ドキュメントで確認すること。

### Croの判断・実施
`pv_pipeline/` にCLIとして実装。カット単位で再実行でき、`cuts.yaml` の差し替えだけで
別物件に流用できる構成にした。工程3（目視確認）は stage2 への進入ゲートとして実装。

### 環境の制約（重要）
このクラウド実行環境の egress ポリシーが以下を拒否する。
- `api.openai.com` → CONNECT 403（画像編集APIに到達不可）
- `api.minimax.io` / `platform.minimax.io` → 到達不可
- `developers.openai.com` / `ai.google.dev` / `docs.cloud.google.com` → 到達不可
- 到達できたのは `generativelanguage.googleapis.com` と `cloud.google.com` のみ

→ **stage1（人物合成）と stage2（動画化）の実行はローカルでやる必要がある。**
　 パイプラインの配線は mock プロバイダで通しで検証済み（`scripts/selftest.sh`）。

### 確認したエンドポイント
- 画像: `POST https://api.openai.com/v1/images/edits`、モデル `gpt-image-2.5-sunburst`
  （2026-09-08リリース）。サイズ `2048x1152` は 16:9 かつ 16の倍数制約を満たす。
- 動画: `POST https://generativelanguage.googleapis.com/v1beta/models/{model}:predictLongRunning`
  → `GET /v1beta/{operation.name}`。Gemini API 公式ディスカバリドキュメントで確認済み。
- MiniMax H3: 公式ドキュメントに到達できず**未検証**。config で差し替え可能にして明示。

### 決定事項
1. 完成尺は約31.5秒になる（36秒 − ディゾルブ0.5秒×9）。35秒に寄せるなら
   `xfade_duration` を下げるか `duration` を足す。ほせもやんの判断待ち。
2. 音楽は無音で出力する。BGMを単体生成する公開APIが無いため（Gemini API v1beta の
   ディスカバリに音楽生成モデルは存在しない）。音源を渡せば `--music` で載せられる。
3. Drive の素材は HEIC のままなので、パイプライン側で HEIC→JPEG 自動変換を入れた。
4. cut01 は「人物なし」だが画像編集は通す（16:9化のため）。cut08 は指示どおり編集スキップ。

### 次のアクション（ほせもやん）
- `【施設名】` を確定して `cuts.yaml` に入れる
- ローカルに clone し、`.env` にキーを入れて `python run.py --stage 1` を回す

---

## 追加指示：ChatGPT Image 連携

### ほせもやんからの指示
画像の合成は ChatGPT Image でやりたい。そこの連携設定を取る。

### Croの実施
1. `python run.py connect` を追加。キー → ホスト到達性 → モデル利用可否 → 実編集、の順に
   切り分けて、どこで止まっているかを言い切る。`--smoke` で実際に1枚編集して確かめられる
2. `auth` 設定を追加。`bearer`（自分で認証）と `proxy`（クラウド環境の API credentials に
   注入させる）を切り替えられる
3. `size` の事前検証を追加。16の倍数・総画素数・アスペクト比の制約を API を叩く前に判定する
   （`1920x1080` は 1080 が16の倍数でないため不可。`2048x1152` を採用）
4. HTTPエラーを日本語で言い切るようにした（401/403/404/429、response_format、input_fidelity）

### 検証
ローカルにモックAPIサーバを立て、stage1 を実プロバイダ経路で通した。
参照画像の枚数が 02:2枚 → 03:3枚 → 05:4枚 → 04:5枚 → 07:6枚 → 09:7枚 と
連鎖して増えることをサーバ側の受信数で確認済み。出力は 16:9。

### 確認したAPI仕様
- `response_format` は gpt-image 系では400。送らない
- `input_fidelity` は gpt-image-2 以降では指定不可。入力を常に高忠実度で処理するため、
  「建物は変更禁止」はモデル既定の挙動で満たされる
- `quality` は low/medium/high に加え、2.5系は xhigh / max も使える
- レスポンスは常に `data[0].b64_json`

### クラウドで繋ぐ場合の手順（ほせもやんの作業）
claude.ai/code のメッセージボックス上の雲アイコン → 環境の歯車 → 環境ダイアログ。
- 方法A（推奨・Pro/Max）: API credentials に `api.openai.com` / `Authorization: Bearer <key>`
  を登録。キーがVMに入らず、ホストはネットワーク許可リストを迂回する。
  **`config.yaml` を `auth: proxy` に変えること**（忘れると認証ヘッダが二重になる）
- 方法B: Network access を Custom にして Allowed domains に `api.openai.com` を追加し、
  Environment variables に `OPENAI_API_KEY` を置く。キーはセッションから見える

---

## ヴィラPV：建物構成の確定（cut10）

### ほせもやんからの指摘
- 右棟の黒い横長パネルは屋外ビジョン（プールサイドで動画を流す画面）。黒で正解
- 右棟の暗い扉は倉庫
- リビングは玄関ドアのある棟のさらに左の平屋。cut05・cut07 の部屋
- 参考写真5枚をデスクトップに置いた（IMG_5591 / 5654 / 7810 / 8007 / 8557）

### Croの判断・実施
- クラウド側で写真を見ずに入れた 2a6505d（右棟をガラス張りのリビングとして描かせる指示）は誤りだった。撤回
- cut10 の元写真 IMG_5577 はリビング棟が左の画面外に切れている。16:9 拡張の左余白にリビング棟を描かせるため、
  同じ階段からの夜景 IMG_8007 と、リビング棟の大窓 IMG_5654 を参考画像として添付（input/ref_*.jpg）
- 15:24 生成版で OK をもらった

### 教訓
写真を見ずに建物の指示を書かない。ローカル実行に切り替えてからは、生成物と元写真を必ず並べて確認してから報告する。

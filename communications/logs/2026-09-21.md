# 2026-09-21 — 管理台帳の所在調査

## ほせもやんからの相談

`/kanri`（フジヒサハウジング管理台帳コマンド）が発火しない。
同日に行ったローカル環境の整理（デスクトップ `.claude` ×2、consul-slide、`G:\pv`、
slide-method、`G:\venvs`、スキル・コマンドのジャンクション集約）が原因ではないか。

## Croの調査

### 1. リポジトリ内 — ヒット0件

- `kanri` / `台帳` / `daicho` / `ledger` — 全滅
- `.claude/commands/` は**ディレクトリごと存在しない**
- `.claude/skills/` は ai-editorial / ai-employee / score-rename / x-skill-scout の4本のみ
- `フジヒサ` のヒット3件はすべて賃料相場レポートの署名行のみ

### 2. ローカル整理との関連 — 無関係と判定

このセッションはクラウド実行。ローカルディスクは一切見えない。
移動作業が原因で `/kanri` が消えたのではなく、**ここには最初から無い**。

### 3. Google Drive — 発見

ほせもやんの指摘（「gドライブのgoogle drive_jhの中におらんか」）を受けてDrive検索。

**共有ドライブ ▸ フジヒサ ▸ `rentbook_data`** に台帳データ一式を確認。

| 対象 | 更新 |
|---|---|
| `rentbook-before-rewrite-20260910.bundle`（582KB / gitバンドル） | 09-10 |
| `台帳_投入SQL/`（SQL 26本 + `daicho_payload.json` + `extract.py` / `gensql.py`） | 09-03 |
| `rentbook-data-backup/` | 07-24 |
| `収支マイナス物件の原因報告_2025-2026年度.pdf` | 09-11 |
| `BM比率_2025-2026年度.pdf` | 09-11 |

投入SQLから判明した管理物件：
プランドール堂島／阿波座／道頓堀、ルネスプランドール守口、シャーメゾン新大阪、
近畿吉田ビル、富士マンション、東大阪松原、東中浜、五月田町、大庭町、豊野町、川西市久代

### 4. `G:\ClaudeLocal` の正体

Drive内に `ClaudeLocal` フォルダは**存在しない**。
つまり `G:\ClaudeLocal` はGoogle Driveではなく**ローカルディスクのG:ドライブ**。
今日移動させた `G:\pv` `G:\venvs` と同じドライブであり、巻き込まれた可能性は否定できない。

## 決定事項

**台帳の読み書きは今後すべて Drive の `rentbook_data` に対して行う。**
ほせもやん指示：「必要なものはここに全て移してるので今後はここを読み書きしにいってほしい」

書き込み権限 `canAddChildren: true` を確認済み。

## 次のアクション

- [ ] 台帳DB本体の所在を特定（Supabase の可能性 — ほせもやんの許可待ち）
- [ ] `rentbook-before-rewrite-20260910.bundle` を展開し `/kanri` 定義の有無を確認
- [ ] `/kanri` を全環境で使えるようにするか判断（台帳本体の所在確定後）

## 併せて報告した構造の問題（未対応）

`kansai-housing-intel/Crossactor/` にリポジトリ全体が入れ子で複製されている。
`ceo_system` も `skills/development.md` も二重に存在。今日の「slide-method 重複集約」と同種の問題。
対処方針は保留中。

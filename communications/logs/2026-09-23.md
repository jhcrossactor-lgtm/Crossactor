# 2026-09-23

## note自動投稿CLI「note-post」構築

**指示（ほせもやん）**
Node.js＋Playwrightでnote自動投稿CLIを作る。login / run（topics.csv→Claude生成→自己チェック→公開→posted.csv）、1日1本・STOP・--dry-run、失敗時Slack通知、セレクタはselectors.jsonに分離。まず login と --dry-run の下書き保存までを動作確認。

**Croの実装判断**
- 配置: `tools/note-post/`
- --dry-run = note下書き保存まで行い公開しない。topics.csvは未処理のまま、1日1本の枠も消費しない（posted.csvには `dry_run` で記録）
- 自己チェックNG（本番）も1日1本の枠を消費する（同じトピックでAPIを連打しないため）
- 本文はMarkdown→HTMLに変換し、エディタへ貼り付けイベントで流し込む（見出し・リスト・太字を保持）
- モデル: claude-opus-5（環境変数で変更可）、拒否時サーバー側フォールバック有効

**検証結果**
- クラウド環境ではnote.comへの通信がegressで403、APIキーも未設定 → 実画面・実APIでは未検証
- ダミーnote画面＋モックLLMで6ケース全通過（login / dry-run下書き / 公開+URL記録+1日制限 / NG→下書き / STOP / ログイン切れ / セレクタ不一致）
- selectors.jsonの値は実画面で未確認（確証なし）→ ローカルで `NOTE_HEADLESS=0 ... run --dry-run` による確認が次のアクション

## note-post に差し込み画像（ChatGPT / OpenAI）を連動

**指示（ほせもやん）**
差し込み画像も作りたいのでChatGPTと連動させる。

**Croの実装判断**
- OpenAI Images API（公式SDK `openai`）、既定モデル `gpt-image-2`、1536x1024、quality medium
- 画像の位置と内容はClaudeが記事生成時にマーカーで指定 → 自己チェックで画像指示も審査
- 画像はチェックOK時だけ生成（NG記事に費用をかけない）。1枚失敗しても記事は投稿を続ける
- noteエディタの「+」→「画像」→ファイル選択で差し込む。セレクタは selectors.json の `image` に追加（実画面未検証・確証なし）
- アイキャッチ（見出し画像）は今回対象外

**検証結果**
ダミー画面で7ケース全通過（マーカー位置への差し込み、NG時は画像なし、画像0枚設定を含む）。OpenAI実APIは未検証（キーなし）。

## note-post を「在庫＋人の最終チェック」方式に変更

**指示（ほせもやん）**
基本は自動だが、投稿前の最終チェックは自分でやりたい。→ 承認方式は「承認コマンドで即公開」を選択。
途中で方針変更：確認待ちが残っていても下書きは作り続け、在庫の中から選んで公開したい。

**実装**
- `run` は公開しない。毎日1本、下書き保存して在庫（posted.csv の pending / pending_ng）に積み、Slackに番号付きで通知
- `list` で在庫一覧、`publish <番号>` で公開、`reject <番号>` で見送り
- 公開は note 上の下書きをそのまま出す（ほせもやんの手直しを上書きしない）
- 自己チェックNGの在庫は `--force` なしでは公開できない
- 1日1本は生成・公開それぞれに適用

**検証結果**
ダミー画面で9ケース全通過（在庫積み上げ・番号指定公開・note上の手直し保持・NG在庫/--force・reject を含む）。
